# Se crea una lista llamada 'numeros' con varios valores enteros
numeros = [8, 15, 3, 22, 7]

# Se elimina el número más grande de la lista (en este caso, el 22)
numeros.remove(max(numeros))

# Se imprime la lista actualizada, sin el número más grande
print(numeros)